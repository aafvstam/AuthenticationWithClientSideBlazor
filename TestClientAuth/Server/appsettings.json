{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // SQLite
    // Comment if you want to use this repo on your local SQL Server instance instead of SQLite
    // ----------------------------------------------------------------------------------------
    "DefaultConnection": "Filename=./Data/TestClientAuth.db"

    // SQLite on Azure
    // "DefaultConnection": "Filename=D:/home/site/wwwroot/Data/SoftAsInSoftware.db"

    // SQL Server (localdb)
    // Uncomment if you want to use this repo on your local SQL Server instance instead of SQLite
    // ------------------------------------------------------------------------------------------
    // "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=AuthenticationWithClientSideBlazor;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  "Authentication": {
    "JwtSecurityKey": "RANDOM_KEY_MUST_NOT_BE_SHARED",
    "JwtIssuer": "https://localhost",
    "JwtAudience": "https://localhost",
    "JwtExpiryInDays": 1
  },

  "AllowedHosts": "*"
}
